{
  "dependencies": {
    "express": "^5.1.0",
    "jest": "^30.2.0",
    "nodemon": "^3.1.11"
  },
  "type": "module",
  "scripts": {
    "test": "set NODE_OPTIONS=--experimental-vm-modules && npx jest"
  }
}
